.related-articles
  %h1 <i class="fi-star hero-color"></i> RELATED ARTICLES
  %hr
  .row
    .columns.small-12
      %ul.small-block-grid-2.large-block-grid-3
        - related_articles.each_with_index do |article, index|
          %li
            = link_to (image_tag article.image_url(:thumb).to_s), article_path(article)
            %p.no-margin= link_to article.name, article_path(article)
            %p.no-margin= strip_tags (truncate_html((markdown article.content), length: 150))
            %p
              %em= link_to article.category.name, category_path(article.category_id)
